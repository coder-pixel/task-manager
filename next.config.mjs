/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: [
    "@mui/material",
    "@mui/system",
    "@emotion/react",
    "@emotion/styled",
  ],
};

export default nextConfig;
